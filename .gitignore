# Add any directories, files, or patterns you don't want to be tracked by version control
*.dcu
*.bpl
*.json
__history/
__recovery/
Win32/
/modules

# Boss package manager files
!boss.json
!boss-lock.json

# PubDelphi manifest (tracked, like boss.json)
!pubdelphi.json

# Docusaurus (docs-src/) — local artifacts; official build on GitHub Actions
docs-src/node_modules/
docs-src/build/
docs-src/.docusaurus/
